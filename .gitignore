node_modules/
dist/
.next/
next-env.d.ts
.turbo/
*.tsbuildinfo

# Pinned QMK checkouts are large, reproducible build inputs — never committed.
.cache/

# Generated catalogs and build workspaces
/catalogs/
/var/

.env
.env.local
.DS_Store

# Linked worktrees and local agent state are never committed.
.claude/

# GSD workflow tooling runtime state — never committed.
.gsd/

# GSD ephemeral session lock (pid/session id) — never committed.
.planning/*.lock
